"""
CoinGlass Provider (opcional, placeholder).
Regras do DOCX: sem scraping agressivo; usar timeout/retry; cache.
"""
def get_coinglass_metrics(symbol: str) -> dict:
    return {}  # implemente se tiver API/limites
